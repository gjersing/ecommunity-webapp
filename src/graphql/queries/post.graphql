query Post($id: Int!) {
  post(id: $id) {
    id
    body
    img
    points
    comments {
      id
      body
      username
      userId
      createdAt
    }
    likeStatus
    createdAt
    author {
      id
      username
      email
    }
  }
}
